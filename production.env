# =============================================================================
# Production Environment Variables Template
#
# This file contains environment variables for both your Render backend
# and Vercel frontend.
#
# IMPORTANT: 
# - Replace all placeholder values with your actual configuration
# - Do NOT commit sensitive information directly to your repository
# - Set these in your Render/Vercel dashboards, not in this file
# =============================================================================


# =============================================================================
# RENDER (Backend) - Set these in Render Dashboard → Your Service → Environment
# =============================================================================

# Node.js environment mode
NODE_ENV=production

# Port for the Express server (Render sets this automatically, but include as fallback)
PORT=3000

# Session secret for Express sessions
# ⚠️ GENERATE A NEW, UNIQUE, RANDOM STRING FOR PRODUCTION!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_super_secret_session_key_here_replace_me_with_a_long_random_string

# The URL of your deployed Vercel frontend
# Used for CORS and OAuth redirects
FRONTEND_URL=https://anime-dashboard-green.vercel.app

# Your PostgreSQL database connection string from Render
# ⚠️ Get this from: Render Dashboard → Your PostgreSQL Database → "Internal Database URL" or "Connection Pooling"
# Format: postgresql://user:password@host:port/database_name
DATABASE_URL=postgresql://user:password@host:port/database_name

# Base URL of your backend (for OAuth redirects)
# Usually the same as your Render service URL
BASE_URL=https://anime-dashboard-api.onrender.com


# =============================================================================
# VERCEL (Frontend) - Set these in Vercel Dashboard → Your Project → Settings → Environment Variables
# =============================================================================

# Node.js environment mode
NODE_ENV=production

# The base URL of your deployed Render backend API
# ⚠️ CRITICAL: This tells the frontend where to send API requests
API_BASE=https://anime-dashboard-api.onrender.com

# Dashboard title (displayed in the frontend header)
DASHBOARD_TITLE=My Anime Dashboard

# Dashboard subtitle (displayed in the frontend header)
DASHBOARD_SUBTITLE=Visualize your anime watching journey.

# Your Google Gemini API Key (if using AI features)
# ⚠️ Keep this secret! Get it from: https://aistudio.google.com/app/apikey
# If not using Gemini AI features, leave this empty
GEMINI_API_KEY=

# Number of anime items to display per page in lists
EPISODES_PER_PAGE=25

# Maximum number of genres to show in charts
CHART_GENRE_LIMIT=10

# The Gemini model to use for AI features
# Options: gemini-2.5-flash (fast, recommended) or gemini-1.5-pro (more accurate)
GEMINI_MODEL=gemini-2.5-flash


# =============================================================================
# NOTES:
# =============================================================================
#
# RENDER (Backend):
# 1. Go to: https://dashboard.render.com → Your Service → Environment
# 2. Add each variable above (except Vercel ones)
# 3. For DATABASE_URL: Get it from your Render PostgreSQL database dashboard
# 4. For SESSION_SECRET: Generate a new random string (see command above)
# 5. Save changes (triggers automatic redeploy)
#
# VERCEL (Frontend):
# 1. Go to: https://vercel.com/dashboard → Your Project → Settings → Environment Variables
# 2. Add each variable above (except Render ones)
# 3. Set for: Production, Preview, and Development (select all three)
# 4. Save changes (triggers automatic redeploy)
#
# SECURITY:
# - Never commit this file with real values to Git
# - Use strong, unique values for SESSION_SECRET
# - Keep GEMINI_API_KEY secret
# - DATABASE_URL contains sensitive credentials
#
# TESTING:
# After setting variables, test:
# - Frontend: Visit https://anime-dashboard-green.vercel.app
# - Backend: Visit https://anime-dashboard-api.onrender.com/health (if you have a health endpoint)
# - Check browser console for API errors
# - Verify window.CONFIG.API_BASE is set correctly

